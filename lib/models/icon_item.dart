import 'package:flutter/material.dart';

class IconItem {
  String name;
  String latestAccess;
  IconData icon;
  String version;

  IconItem(this.name, this.latestAccess, this.icon, this.version);
}
